In the project root directory (which contains this README and the top
CMakeLists file), run:
`cmake -S . -B build/`.
This will place all build objects (including executables) in a
"build" subdirectory inside the root.